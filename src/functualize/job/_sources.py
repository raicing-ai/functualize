"""Sources capability — the resolved inputs a job's own ``Fingerprint`` declared.

Re-exports from the canonical implementation in _engine/capabilities.
"""

from functualize._engine.capabilities.sources import Sources

__all__ = ["Sources"]
