"""Provider protocol for the {{ display_name }} domain."""

from __future__ import annotations

from typing import Protocol, runtime_checkable


@runtime_checkable
class {{ class_name }}Provider(Protocol):
    """Protocol that {{ domain_name }} implementation plugins must satisfy."""

    ...
