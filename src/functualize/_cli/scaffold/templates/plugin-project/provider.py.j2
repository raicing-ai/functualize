"""InputProvider implementation for {{ project_name }}.

Implements the InputProvider protocol via structural typing (no inheritance
required). Register via the functualize.plugins entry point in pyproject.toml.
"""

from typing import Any


class {{ class_name }}Provider:
    """Custom input provider for functualize.

    Satisfies the InputProvider Protocol via structural typing.
    Register via the functualize.plugins entry point in pyproject.toml.
    """

    name: str = "{{ package_name }}-provider"

    def provide(self, prompt: str, **options: Any) -> Any:
        """Collect input from the user or an external source.

        Args:
            prompt: A description of what input is needed.
            **options: Additional provider options.

        Returns:
            The collected input value.
        """
        return input(prompt)
