"""Plugin boot and registration for {{ project_name }}."""

from __future__ import annotations

from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from functualize.plugin import PluginHost


class {{ class_name }}Plugin:
    """{{ description }}

    Registers the {{ provider_name }} provider for the {{ domain_name }} domain.
    """

    name = "{{ provider_name }}"
    domain = "{{ domain_name }}"

    def __call__(self, app: PluginHost) -> None:
        """Boot the plugin and register with the DI registry.

        Args:
            app: The host, as the plugin port -- eleven members, and a typo in
                any of them is a type error rather than an `AttributeError`
                inside whatever `except` happens to be in scope.
        """
        # TODO: Register your provider implementation with app.di.provide()
        pass
