Specify phase for: $ARGUMENTS

1. Read `.spec/STATE.md` — if absent, treat as: no work in flight
2. Create `.spec/features/<name>/` if it does not exist
3. Write `spec.md` — problem statement, user stories, behavior, acceptance criteria (behavior only, no implementation)
4. Write `contracts.md` — external interfaces only: component props, API response types, event payloads, exported function signatures. NOT database schemas or internal types.
5. Get user confirmation before proceeding to Plan
