# Utilities

A scratch directory for ad-hoc tooling. Nothing is set up here yet.

functualize is available as a local source checkout, at the path recorded in
`functualize-path.txt`. Depend on it by that path rather than from PyPI — the
published release is older than this checkout.
