# Drift — `c0c921f` → `e57f0c9`, claim by claim

The entrypoint audits were verified at `origin/master` @ `c0c921f` on 2026-09-08. This is
the re-run at `2a079de` (0.3.0) on 2026-09-09. PR #36 landed after, touching no `src/` file,
so these results hold at `e57f0c9`.

**Result: 15 STILL HOLD · 2 DRIFTED · 0 FALSE.**

---

## The files did not move

Blob-hash comparison, `c0c921f` vs `2a079de`:

```
SAME  src/functualize/_engine/executor.py
SAME  src/functualize/app/core.py
SAME  src/functualize/_app/boot.py
SAME  src/functualize/_app/impl.py
SAME  src/functualize/_engine/capabilities/runcontext.py
SAME  src/functualize/_engine/capabilities/stdout.py
SAME  src/functualize/_types/exit_codes.py
SAME  src/functualize/_types/http_status.py
SAME  src/functualize/_config/chain.py
SAME  src/functualize/_cli/tui/job_execution.py
SAME  src/functualize/_discovery/cached_provider.py
```

PR #35 was **+9,418 / −1,294**, almost entirely additive: six new modules
(`app/_workflow_view.py`, `_workflow_answer.py`, `_workflow_control.py`,
`adapters/workflow_flags.py`, `_primitives/scope_store.py`, `scope_format.py`), a large
`_cli/builtins.py` expansion, and a rewritten MCP plugin. It touched no structure either
audit flagged.

## The table

| # | Claim | Verdict | Current | Note |
|---|---|---|---|---|
| 1 | `execute()` is a 12-param procedure taking the resolved function | **HOLDS**, recount | `executor.py:657` | **13** params excluding `self`. `:656` was wrong at `c0c921f` too |
| 2 | The engine resolves by name for its own children | **HOLDS** | `executor.py:1216`, `:1795` | audit said 1214/1793 — off by 2 at both refs |
| 3 | 8 production sites resolve name→function | **HOLDS** | 6 external + 2 engine-internal | identical membership; two moved with the click rewrite |
| 4 | 5 post-hoc private writes on the engine | **HOLDS — exact** | `boot.py:286,498,701,1586`; `core.py:514` | no new sites in `src/` or `plugins/` |
| 5 | `RunContext` reads back through `engine._app` | **HOLDS**, path wrong | `_engine/capabilities/runcontext.py:698` | the cited `_app/runcontext.py` **does not exist**; 14 `_app` reach-throughs total, `:698` the only unguarded one |
| 6 | Engine construction duplicated across boot paths | **HOLDS — exact** | `boot.py:270-288` vs `:482-500` | differ only in a config-view factory; same 20-line comment verbatim |
| 7 | Seven `RunStatus` translation sites | **DRIFTED — 7 → 9** | see below | `builtins.py:604` → `:608`; PR #35 added two |
| 8 | Deposit-protocol reads | **HOLDS**, one moved | `executor.py:1283`; `stdout.py:151,156`; `click_params.py:74` | `_force` read moved 82 → 74. Full enumeration is **7 attributes**, not 3 |
| 9 | `interactivity.job.submit` runs with no `WorkflowScope` | **HOLDS — exact** | `_app/impl.py:845-866`, call at `:861` | four arguments; no `parent_scope`, no `workflow_scope_id` |
| 10 | MCP split three ways on `group_option_values` | **HOLDS**, lines moved | `_server.py:278` passes; `_tools.py:287`, `:450` do not | split survived the PR #35 rewrite intact |
| 11 | LOC budgets breached | **HOLDS** | see below | only `app/utils.py` grew |
| 12 | `negative_flag_for` has 7 consumers | **HOLDS** | `click_params.py` ×4, `bar.py:295`, `sync.py:134`, `dispatch.py:729` | `dispatch.py` moved 736 → 729 |
| 13 | `_config → _events` `TYPE_CHECKING` blind spot | **HOLDS**, off by one | `chain.py:22`, `sources.py:35` | audit said `sources.py:36` |
| 14 | `func why` has a second verdict engine | **HOLDS**, span wrong | `app/core.py:725-823` | `explain()` is 701-724 (prose); `explain_verdicts()` is the second engine |
| 15 | `GroupOptionsConflictError` escapes as a raw traceback | **HOLDS — exact** | `cached_provider.py:916` | **zero** `except GroupOptionsConflictError` in `src/`; only `tests/group_options/test_group_options_discovery.py:171` catches it |
| 16 | Single-file mode's second boot runs CWD top-level code | **HOLDS**, shifted +12 | `_cli/main.py:1652-1668` | PR #35 patched a *symptom* — see below |
| 17 | `app/utils.py` is a 2,021-LOC corridor | **DRIFTED — grew** | **2,068 LOC, 93 re-exports** | was 2,021 / 75. **+47 LOC, +18 re-exports in one release** |

## Claim 7 expanded — the two new sites

| # | Site | Added |
|---|---|---|
| 1–2 | `_types/exit_codes.py:50-68`, `_types/http_status.py:53-75` | the tables |
| 3 | `app/adapters/click_params.py:1271-1276` (`deliver_job_result`, def `:1194`) | |
| 4 | `_cli/tui/job_execution.py:271-285` | own success set |
| 5 | `_cli/builtins.py:589-608` | own success set at `:598` |
| 6 | `_cli/builtins.py:1354-1367` — `_resume_exit()` | **PR #35** |
| 7 | `plugins/functualize-mcp/.../_tools.py:35-48` — `wire_status()` | **PR #35** |
| 8 | `plugins/functualize-http/.../__init__.py:193` | |
| 9 | `plugins/functualize-lambda/.../__init__.py:65` | |

`git grep "def wire_status" c0c921f` → empty. Its docstring reads *"Three doors disagreed."*

## Claim 11 expanded — LOC at `2a079de`

| Target | Span | LOC | Budget |
|---|---|---|---|
| `class RunContext` (`_engine/capabilities/runcontext.py`) | 117–898 | **782** | ≤ 500 |
| `class FunctualizeApp` (`app/core.py`) | 64–1328 | **1,265** | ≤ 300 |
| `class JobExecutionEngine` (`executor.py`) | 157–2557 | **2,401**, 55 methods | "> ~500 → decompose" |
| └ `_execute_lifecycle` | 757–1085 | **329** | — |
| `app/utils.py` (file) | — | **2,068** | — |
| `_cli/builtins.py` (file) | — | **2,726** | — |

Files unchanged from `c0c921f` except `app/utils.py` (+47) and `_cli/builtins.py`.

## Claim 16 expanded — PR #35 patched the symptom

`_cli/main.py:1544-1560` gained:

```python
if app.get_job(peer_name) is not None:
    continue
```

> *"Running a file that lives in the current directory meant directory discovery had already
> registered its peers, and this loop then registered them a second time: `func weather.py
> trip_planner` from inside `weather.py`'s own directory died with an unhandled `ValueError`
> and a traceback."*

Plus a regression test, `tests/cli/test_single_file_in_own_directory.py` (96 lines). The
second boot is still there; it is now shipping bugs and getting local fixes.

## New since the audit

| | |
|---|---|
| A **ninth execution door** | `app/_workflow_control.py:176` — `guarded_execute` → `app.execute`. Routes via `core.py:621`, so it adds **no** name→function resolution site. It is the funnel all three workflow surfaces execute through |
| The deposit protocol is **7 attributes** | `_prompt_gates`, `_output_format`, `_force`, `_app`, `_surface_stack`/`_surfaces`, `_ambient_constructs`, `_event_bus`. All 10 writes are in `_cli/main.py`; 6 are read through `getattr(..., default)` — a silent default, so a door that forgets to deposit does not fail |
| **Three** `Path.cwd()` sites in the kernel | `executor.py:1096`, `preflight.py:116`, `runcontext.py:291` — audit reported one |
| A job cannot act on its freshness verdict | `executor.py:1026` returns before the body runs |
| The `entry_points()` memoization already shipped | `84ed555` (PR #4), 2026-08-27. Recommendation withdrawn |
| `RunStatus` has no `.ok` | `_types/enums.py` has `.resumable` (`:35`) and `.ran` (`:47`). `.ok` is on `WalkReport` (`workflow_walker.py:138-141`) |

## Test infrastructure, confirmed present

| | |
|---|---|
| `tests/perf/test_startup_budget.py` | 208 lines, `class TestStartupBudget:95`, `@pytest.mark.perf_budget`, skipped under coverage or xdist |
| `tests/engine/test_lifecycle_order.py` | 287 lines. AST-walks `_execute_lifecycle` and checks call order against `execution-lifecycle.md` |
| `TestWarmBootParity` | `tests/integration/test_declared_capabilities_e2e.py:540` |
| `cli_run` dual-surface harness | `tests/conftest.py:454`, parameterised over `func` \| `app` |
