; Base configuration — always loaded.
; The ENVIRONMENT variable (default: dev) selects an overlay file
; (config.dev.ini, config.prod.ini, ...) merged on top of this one.
[forecast]
api_url = https://weather.example.com
days = 3
