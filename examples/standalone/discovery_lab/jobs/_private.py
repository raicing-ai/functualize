"""Never discovered — the underscore-prefixed filename fails the default
module pre-filter in every mode, including baseline convention."""


def secret() -> str:
    """You should never see this in a job listing."""
    return "how did you run this?"
